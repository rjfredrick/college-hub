specVersion: "v1beta"
serviceId: "college-hub"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "college-hub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
